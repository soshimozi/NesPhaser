class Bus {
    CpuRead(address: number, isReadOnly: boolean): number {
        // Implement your read logic here
        return 0;
    }

    CpuWrite(address: number, data: number): void {
        // Implement your write logic here
    }
}

export default Bus;